[pytest]
# The crawler service imports modules as top-level names (``from models …``,
# ``from config …``), so pytest must treat apps/crawler as the rootdir.
testpaths = tests
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
