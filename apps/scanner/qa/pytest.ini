[pytest]
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    slow_network: tests that require external network targets (DAST/PENTEST_FULL); opt-in
filterwarnings =
    ignore::DeprecationWarning
addopts =
    -ra
    --tb=short
    --strict-markers
