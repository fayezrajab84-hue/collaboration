fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.0
pydantic-settings==2.2.1
httpx==0.27.0
semgrep>=1.70.0
checkov>=3.2.250
# setuptools<81 required: semgrep's opentelemetry dependency uses pkg_resources
# which was removed in setuptools>=81. Pin to last version that ships it.
setuptools==75.8.2
# Playwright: headless Chromium for true SPA crawling (React/Angular/Vue)
# Captures network requests made by JS frameworks that static crawlers miss.
playwright==1.44.0
# Phase 29 — CSPM (Prowler) is NOT installed here. Prowler's Azure SDK
# dep tree conflicts with semgrep/checkov pin sets (pip backtracks across
# every semgrep version trying to resolve). Lives in a dedicated container
# `scanner-cspm` with its own minimal dep set (apps/scanner/requirements-cspm.txt).
# The cloud_azure scanner module imports here resolve via standard Python
# path; the entry point is gated by SCANNER_MAP[ScanType.CLOUD] which is
# only reachable from the scanner-cspm container.
# QA harness — smoke + contract tests for all scanners. See apps/scanner/qa/.
pytest==8.2.0
